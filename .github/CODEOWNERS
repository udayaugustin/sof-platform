# v0: human board (@udayaugustin) is the explicit CODEOWNER for all paths.
# The original org-team handles (@software-company-ai/cto, /ceo) don't exist as
# GitHub teams on udayaugustin/sof-platform, which silently disabled the
# codeowner-review requirement. See SOF-23 / SOF-25.
*               @udayaugustin

# Architecture decisions
/docs/adr/      @udayaugustin

# CI/infra
/.github/       @udayaugustin
/fly.toml       @udayaugustin
